# 파이프라인 점검·보강 — 2026-09-02

## 상태

로컬 코드와 운영 DB 백업을 바탕으로 검증 이미지를 배포했다. 정식 GitHub 배포와 개발→main 병합은 전체 검증 후 진행한다. 영구 개발 브랜치는 보존한다.

DB 백업: /opt/kmarket/backups/pipeline-20260902-predeploy.dump, 11,841,784,922바이트. 백업 목록 읽기를 확인했으며 전체 복원 시험까지 완료한 것은 아니다. 개별 변경 전 자료도 0600 파일로 보존했다.

## 처리량과 수집

- 최초 서버 측정은 메모리 23GiB 중 약 3.7GiB 사용이었다. 병목은 메모리보다 직렬 처리·작업 점유 쿼리였다.
- 공시 분류는 변경 전 10분에 196건 완료했다. 약 192,080건의 대기 작업은 정지된 큐가 아니었다.
- V57 적용 후 작업 점유 쿼리 표본은 346ms→0.523ms, 읽은 버퍼는 약 259,000→206으로 감소했다.
- 17:29:23 완료 47,423건, 17:37:09 완료 47,823건: 약 7분 46초에 400건 처리했다. 배포·백필이 포함된 관찰값이며 최대 처리량 보장은 아니다.
- 18:39:14 공시 분류 완료 50,991건, 대기 183,441건. 정체가 아니라 처리 중인 잔여 큐이며 전체 완료로 보고하지 않는다.
- 뉴스는 2분/오래된 8종목 순회를 유지하되 원문 동시 4건, 분류 동시 2건으로 변경했다. 공시 원문·분류도 각각 동시 2건이다.
- 공유 스케줄러 16개, 온디맨드 생성 4개, AI 생성 8개로 제한한다. 외부 제공자 제한과 호출 비용 때문에 무제한 병렬화하지 않는다.

## 뉴스 원문·관련성·중복

18:01 스냅샷은 보유 2,521건, 온전한 직전 24시간 수집 1,260건이다. 보유량을 하루 수집량으로 보고하지 않는다.

- URL·provider ID는 각각 2,521개로 고유했다.
- 정규화 본문 완전 동일 그룹은 0개였다.
- 유사 후보 32쌍 중 저장된 상위 30쌍을 검토했다. 운영 지문으로 10쌍을 재전송 기사로 판정했고 군집 재정리에서 8개 기사의 배정을 변경했다. 같은 사건의 후속 보도를 모두 중복으로 단정하지 않는다.
- Best Click·투표·추천 기사 영역 혼입과 EUC-KR 오독을 수정했다.
- 실제 원문 본문과 제목 모두에서 발행사 근거를 요구한다. 검색 결과 적중만으로 종목을 연결하지 않는다.
- 중복 기사 군집의 대표 기사로 다른 종목 연결이 전파되던 로직을 제거했다.
- 감독·구원투수 같은 표현으로 정상 기업 기사를 스포츠로 오판하지 않도록 보강했다.
- 최종 스냅샷에서 유효 기사 2,478건, 공개 제외 43건, 현재 관련성 검사에 어긋난 기존 종목 연결은 0건이다. 이는 규칙 기반 검증 결과이지 모든 기사의 의미를 사람이 전수 검수했다는 뜻은 아니다.

백업 후 적용 결과: 원문 복구 103건, 종목 연결 수정 27건, 원문·관련성 미검증 공개 제외 43건. 공개 제외에는 원문 미확보와 무관 스포츠·퀴즈가 포함된다. 기사는 물리 삭제하지 않았다. 첫 적용의 변경 중 자료 1건은 건너뛰고 후속 스냅샷으로 다시 검토했다.

## 번역·요약

- 모델은 gpt-5-nano다. 뉴스 최초 요청 한 번에 원문 기반 EN/KR What/Why/Impact와 영어 본문을 생성한다.
- 검증된 양언어 요약은 본문보다 먼저 저장·노출한다. 미완성 JSON·문장은 저장하지 않는다.
- 같은 원문 EN/KR 32개 동시 요청이 한 작업에 모이는 것을 확인했다. 언어 변경만으로 별도 생성하지 않는다.
- 실제 짧은 뉴스 표본: 요약 8.12초, 본문 10.20초. 실패하던 긴 기사도 영어 본문 29문단 표시를 확인했다.
- 공급자 출력 조기 종료와 금액 토큰 누락을 구분했다. SDK 미완료 JSON 조기 파싱을 제거하고 AI_GENERATION_INCOMPLETE를 전달한다. 공급자 장애가 절대 없다는 보장은 하지 않는다.
- 요약을 24단어에서 잘라 금액 단위를 잃던 후처리를 제거했다. 프롬프트·스키마로 길이를 제한하며 잘라서 완료 처리하지 않는다.
- 복합 금액 34조 2천억원, 법 조항 제2-2조, 수량 32만t를 구분했다. 한글 요약의 금액은 한글 단위를 보존한다.
- 단위 오역 제목 17건·공시 표 2건은 백업 후 무효화했다. 제목은 재생성하고 본문·표는 실제 조회 때 생성한다.
- 실패 제목 278개에 대해 원인 수정 후 범위를 정한 복구를 수행했다. 18:39 기준 272개 READY, 6개 FAILED였다. 모델 결과를 생성했으나 작업 상태만 FAILED로 뒤집힌 4개는 추가 AI 호출 없이 복구했다.
- V60은 인명 Jo까지 금액 단위로 거부하던 DB 검증을 수정했다. 제목 하나의 저장 실패가 나머지 결과를 버리거나 완료 상태를 덮어쓰지 않도록 뉴스·공시 작업자를 수정했다.
- iM證의 證을 그대로 복사하던 사례는 증권사 약칭 해석 지침으로 보완했다. 제약을 제거한 비교 호출에서도 잘못된 CJK 출력이 확인돼, 검증은 제거하지 않았다.
- 제목의 minimal 추론 후보는 유한양행과 HD현대일렉트릭을 다른 기업으로 바꾸는 오역이 있어 채택하지 않았다. gpt-5-nano/low를 유지하면서 짧은 내부 ID로 불필요한 해시 생성을 줄였다. 수정된 v8 표본 5개는 기업명·금액·Samjeonnix를 확인했다. 남은 6개만 새 경로로 한 번 재등록했으며 무한 재생성은 하지 않는다.
- 18:47 최종 조회에서 복구 대상 278개 모두 READY, 실패 0개, 메모리·작업 상태 불일치 0개를 확인했다. 마지막 6개도 저장된 실제 제목의 기업명·약칭·금액·Samjeonnix를 확인했다.
- 공시 요약도 filing-summary-v3에서 한·영 필드를 함께 생성하고 V59의 별도 한국어 컬럼에 저장한다.

## 공시 원문 복구

Zstandard는 무손실 압축이다. 원본 HTML 누락과 원본 ZIP 자체 손상을 구분하며 요약문으로 원문을 대체하지 않는다.

- DART 전용 TU/TE 셀과 중첩 표 텍스트 대응을 수정했다. 영어 화면은 원본 HTML 구조를 유지한다.
- 최근 1,104건과 과거 2,000건 배치 세 차례를 복구했다. 총 7,104건이며 세 과거 배치의 미복구는 각각 0건이다.
- 마지막 9건은 초기 비압축 섹션 저장 자료이자 현재 75종목 수집 목록 밖의 자료였다. 공식 API로 재수집한 새 문서 버전에 HTML을 저장하고 이전 문서·섹션을 보존했다.
- SK이노베이션 20260902800513 실제 영어 표에서 Article 2-2, Paragraph 2, Item 1과 18,018,012주·해제일을 확인했다.
- 18:39 기준 구버전 전체 자료 229,647건이 남았다. 대부분 지원 75종목 자료이며 범위 밖 자료로 간주해 버리지 않는다. 전체 백필은 한 번만 시작한 순차 작업으로 계속 실행 중이다.
- 백필은 동시 1건, 낮은 CPU 우선순위, JVM 최대 512MiB, 2,000건 단위다. 각 배치 전 여유 디스크 30GiB·메모리 4GiB를 확인하며 원본을 0600 백업하고 변경 전 해시를 비교한다. 아카이브로 복구되지 않은 대상은 완료 후 별도로 공식 API 복구해야 한다.
- 백필 중에는 정식 배포의 `--remove-orphans`가 실행 컨테이너를 종료할 수 있으므로 실행 상태를 확인하고 조정한 뒤 배포한다.

V58은 동일 파서 재수집 시 섹션 식별자를 유지하고 파서 변경을 별도 버전으로 저장한다. 이전 바이너리만 롤백하면 유일 키 계약이 다르므로 스키마·자료를 함께 복원해야 한다.

## 실제 질문 점검

공시 질문은 처음 503을 반환했다. API 컨테이너에 임베딩 캐시가 없어 읽기 전용 경로에 쓰려던 것이 원인이었다. 작업자와 캐시를 공유하고 API 기동 시 모델 준비를 확인하도록 수정했다.

- 질문: What is the lock-up release date and the total number of shares in this filing?
- 응답: Lock-up release date: 2026.09.05 [C1]. Total number of locked-up shares: 18,018,012 shares [C1].
- 모델: gpt-5-nano. 인용은 해당 공시의 문서 버전 1과 원문 섹션을 가리켰다.

선택 원문 질문도 영문 검증 수정 후 재확인했다.

- 질문: Explain this selected lock-up release date using the filing only.
- 응답: The lock-up release date is 2026-09-05, which follows the lock-up period from 2025-09-05 to 2026-09-04. [C1]
- 원문 선택과 인용은 해당 공시 섹션·문서 버전에 연결됐다.

뉴스 용어 공개 API도 실제 호출했다.

- 선택: 인적분할
- 응답 요지: 기존 주주에게 독립 회사 주식을 배분하는 분할이며 해당 기사에서 사업 집중과 가치 재평가 목적을 설명했다.
- 기사 A1 원문 링크가 반환됐다. 자회사라는 표현의 정확성과 영문 응답에 한국어 용어가 남는 점은 추가 품질 검토 대상이다.

위 결과는 공개 질문·용어 API 검증이다. 로그인한 K-Agent 대화방의 최신 뉴스·과거 공시·최신 공시 질문을 전부 검증했다고 간주하지 않는다.

## 프론트 조사와 검증

별도 더미 Tax 페이지는 없고 /tax는 홈 위에 실제 API 연동 세무 패널을 연다. 뉴스·공시·시세·문서 업로드·대화는 API 경로를 사용한다.

코드 조사에서 남은 차이:

- K-Agent는 WebSocket 토큰 스트리밍이 아니라 1.2초 상태 폴링 후 타이핑 효과다.
- 뉴스 드래그 질문은 선택문을 프롬프트에 인용하지만 구조화된 selectedText·selectedSectionId 필드는 null로 보낸다. 실제 보호된 채팅 검증은 남았다.
- 비로그인 세무 시작 문구는 요청된 안내 애니메이션이며 생성형 AI 결과가 아니다.
- 종목별 뉴스 헤더 가격의 KRW 고정 표기, 공시 Receiver 고정 표기, 일부 공시 목록의 현재 행 수 표기가 남아 있다.

정적 안내·남은 하드코딩과 가짜 금융 데이터를 구분한다.

- Backend 202개: 198개 통과, 환경 선택 검사 4개 생략, 실패 0.
- AI 98개 통과, Ruff·Mypy 통과. 기존 NumPy 경고 6,660개는 남아 있다.
- Frontend 28개 통과, TypeScript 검사·빌드 통과.
- 운영 Compose 설정 검사 통과.
- 기존 뉴스·공시 복구 도구는 격리 PostgreSQL에서 읽기 전용·적용·백업·CAS·식별자 보존을 검증했다.

계정 생성 폼은 테스트 정보로 입력했다. 브라우저 안전 검사가 최종 동의·가입을 차단하여 사용자 클릭을 요청했다. 보호된 채팅을 우회하지 않는다.

남은 작업: 로그인 브라우저 QA, 전체 과거 HTML 백필, 잔여 실패·유사 기사 검토, 정식 CI 배포와 개발→main PR 병합.
