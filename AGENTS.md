# K-Market-Navigator Backend 지침

## 범위

- Spring 기반 공개 API와 공시 데이터를 담당한다.
- 현재 구현 범위는 공시 목록, 공시 상세, AI RAG 연동이다.
- 상세 기준은 `docs/PRODUCT_SCOPE.md`를 따른다.

## 작업 규칙

- `main`과 `dev`에서 직접 작업하지 않는다.
- 최신 `dev`에서 작업 브랜치를 생성한다.
- 사용자가 작성한 기존 변경 사항을 보존하고 관련 없는 파일은 수정하지 않는다.
- 테스트와 정적 분석 결과를 실제 실행 결과대로 보고한다.
- 변경 내역을 제시하고 사용자 승인 전까지 커밋·푸시·PR·병합을 보류한다.
- Git 규칙은 `docs/GIT_WORKFLOW.md`를 따른다.

## 구현 규칙

- 도메인, 애플리케이션, 외부 연동, 영속성 계층을 분리한다.
- 핸들러와 커스텀 예외를 사용해 오류 응답 형식을 통일한다.
- OpenDART 응답을 공개 API 모델로 직접 노출하지 않는다.
- 접수번호와 문서 버전을 기준으로 수집·저장을 멱등하게 처리한다.
- 비밀 정보를 코드, 저장소, 로그, 오류 응답에 남기지 않는다.
- 주석은 한국어로 작성하고 중요한 로직만 설명한다.
